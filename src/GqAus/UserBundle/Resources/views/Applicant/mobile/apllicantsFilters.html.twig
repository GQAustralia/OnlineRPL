<thead>
    <tr>
        <th class="col-xs-8 col-sm-8 {% if (('ROLE_MANAGER' in userRole) or ('ROLE_SUPERADMIN' in userRole)) %} {% else %}sort{% endif %}" data-sort="firstName">Name</th>
        {% if ('ROLE_FACILITATOR' in userRole) or ('ROLE_ASSESSOR' in userRole) or('ROLE_RTO' in userRole) %}
            <th class="col-xs-4 col-sm-4 {% if (('ROLE_MANAGER' in userRole) or ('ROLE_SUPERADMIN' in userRole)) %} {% else %}sort asc{% endif %}" data-sort="daysleft">Days Left</th>
        {% else %}
                <th class="col-xs-4 col-sm-4 {% if (('ROLE_MANAGER' in userRole) or ('ROLE_SUPERADMIN' in userRole)) %} {% else %}sort {% endif %}" data-sort="status">Status</th>
        {% endif %}
            
    </tr>
</thead>

